<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/btnFacebook"
        android:layout_width="fill_parent"
        android:layout_height="70dp"
        android:onClick="publica"
        android:text="@string/btnFacebook"
        android:textSize="30sp" />

    <Button
        android:id="@+id/btnOuvir"
        android:layout_width="fill_parent"
        android:layout_height="150dp"
        android:onClick="btnOuvirClicked"
        android:text="@string/btnOuvir"
        android:textSize="100sp" />

    <Button
        android:id="@+id/btnLer"
        android:layout_width="fill_parent"
        android:layout_height="150dp"
        android:onClick="btnLerClicked"
        android:text="@string/btnLer"
        android:textSize="100sp" />

</LinearLayout>